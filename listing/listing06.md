Что выведет программа? Объяснить вывод программы. Рассказать про внутреннее устройство слайсов и что происходит при передачи их в качестве аргументов функции.

```go
package main

import (
	"fmt"
)

func main() {
	var s = []string{"1", "2", "3"}
	modifySlice(s)
	fmt.Println(s)
}

func modifySlice(i []string) {
	i[0] = "3"
	i = append(i, "4")
	i[1] = "5"
	i = append(i, "6")
}
```

Ответ:
```
[3 5 3]

В функции main создается слайс s с элементами "1", "2", "3".
Этот слайс передается в функцию modifySlice.

Слайс в Go представляет собой динамический массив с информацией о длине и емкости. 
Внутренне слайс состоит из трех компонентов:
1. Указатель на массив данных
2. Длина слайса
3. Емкость слайса

Когда слайс передается в функцию, сам слайс передается по значению, но он по-прежнему
ссылается на тот же массив данных. Таким образом, изменения в элементах слайса видны
как внутри функции, так и снаружи. Однако, если внутри функции происходит изменение
длины слайса (например, с использованием append), то создается новый слайс с новой
длиной. И он не имеет никакого отношения к предыдущему слайсу.

Так происходит и в данном кейсе, мы передаем слайс в функцию modifySlice и происходит следующее:
i[0] = "3" - тут изменяется наш исходный слайс (то есть, он уже выглядит так "3", "2", "3")
Затем вызывается i = append(i, "4"), это создает новый слайс
i[1] = "5" - уже никак не влияет на наш старый слайс, оно изменяет только новый созданный

```
